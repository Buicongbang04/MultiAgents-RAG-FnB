[pytest]
asyncio_mode = auto
testpaths = tests
# Only collect files with the test_ prefix so the existing benchmark/quality
# scripts in tests/ (router_backend.py, hybrid_retrieval.py, ...) are not run.
python_files = test_*.py
python_classes = Test*
python_functions = test_*
